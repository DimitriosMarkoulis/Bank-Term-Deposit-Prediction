# src/config

Shared project configuration.

## Purpose

`config.py` keeps project-level constants in one place so data loading, preprocessing, training, prediction, and result saving use the same paths and split settings.

## Contains

- project root
- data folder
- raw dataset path
- results folder
- target column
- train/validation/test split sizes
- random state
